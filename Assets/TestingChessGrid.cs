using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingChessGrid : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        GridClass grid = new GridClass(8,8);
    }

}
